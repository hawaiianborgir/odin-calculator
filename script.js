function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

function operate(operator, a, b) {
    switch(operator) {
        case adding:
            add(a, b);
        case subtracting:
            subtract(a, b);
        case multiplying:
            multiply(a, b);
        case dividing:
            divide(a, b);
    }
}


let firstNumber = ;
let secondNumber = ;
let operator = ;



